# Audit — 2026-07-03C (orchestration persistence + test runner cleanup)

Immutable snapshot. Scope: verify the current `master` state after the orchestration fix
and add the small local test-runner guard needed to keep backend tests deterministic.

## Headline

The orchestration runtime is no longer dead against real Postgres. It is now **Built and
verified** at smoke level: runtime tables exist, the command registry reflects actual
handlers, all command types are registered, and smoke fails if workflow instances end in
`failed`.

## Evidence

- Existing commit verified: `54c2c2e fix(orchestration): create missing state tables; register all 21 commands; assert workflow health in smoke`.
- `npm run typecheck`: PASS.
- `npm run smoke`: PASS, 14/14 steps. New step confirms `orchestration recorded no failed workflow instances`.
- `npm test`: PASS, 311/311.
- `cd web && npm run typecheck && npm run lint && npm run build`: PASS.
- `cd web && npm test`: FAIL, 8 stale assertions, matching the known queue item.

## What changed in this session

- `scripts/test.ts` now sets:
  - `NODE_ENV=test`
  - `FINDER_BACKGROUND_JOBS=false`
- This prevents in-process orchestration background jobs from polling while the Node test
  runner is tearing down per-test app/database instances.
- Before the guard, the backend suite had 289 passing tests but 4 cancelled test files due
  to pending event-loop work after DB pool cleanup.
- After the guard, backend tests completed cleanly: 311 pass, 0 fail, 0 cancelled.

## Status labels

| Area | Status | Evidence |
|---|---|---|
| Orchestration persistence tables | Built and verified | Migrations present, smoke 14/14, no failed workflow instances |
| Command registry warning | Built and verified | Startup smoke no longer logs unregistered command warnings |
| Backend test runner cleanup | Built and verified | `npm test` 311/311 |
| Frontend typecheck/lint/build | Built and verified | All passed; lint warnings remain |
| Frontend Vitest suite | Not production-ready | 8 stale assertions still failing |

## Remaining queue

1. Fix real-backend Playwright core-flow failures: checkout, inventory receive, invoice pay, logout.
2. Fix stale frontend Vitest tests: `catalogCart.test.tsx` and `reportsDashboard.test.tsx`.
3. Implement or remove mock-only core endpoints.
4. Close RLS enforcement gap.
5. Decide and execute mock-default flip only after staging real-backend deployment is ready.

## Notes

Frontend lint still reports four pre-existing React hook warnings:

- `documents/_components/AllDocumentsTab.tsx`
- `documents/_components/ByTypeTab.tsx`
- `components/terminal/OfflineQueueBanner.tsx`
- `components/terminal/TenderScreen.tsx`

These warnings do not block `npm run lint`, but they should be cleaned during the stale-test
or frontend hardening pass.
