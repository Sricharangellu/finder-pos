# Ascend Audit 2026-07-03D — Stale Frontend Vitest Cleanup

## Scope

Queue item #2 only: refresh stale frontend Vitest assertions in:

- `web/tests/catalogCart.test.tsx`
- `web/tests/reportsDashboard.test.tsx`

This work intentionally avoided queue item #1 resources owned by Claude session A:
e2e specs, ports 3000/3001, backend server, and Postgres 5432.

## What was broken

`cd web && npm test` failed 8 stale assertions:

- 5 failures in `catalogCart.test.tsx`
- 3 failures in `reportsDashboard.test.tsx`

The failures were test drift, not fresh product regressions:

- Catalog tests still expected the old coffee-shop fixture names: `Latte`, `Espresso`,
  `Pastry`, `Butter Croissant`.
- The active mock catalog now serves retail inventory: `Spring Water 500ml`,
  `Orange Juice 1L`, `Potato Chips 150g`, `Mixed Nuts 200g`, etc.
- Reports tests still expected the old tax/net/order-status layout.
- The active dashboard renders spec metric cards plus `Products sold`, `Top salespeople`,
  and `Payment methods`.

## What changed

- Updated catalog assertions to prove current catalog loading, add-product click,
  category filtering, search filtering, and no-results behavior using the active retail
  mock catalog.
- Updated reports assertions to prove current metric cards, payment methods, and backend
  top-products table rendering.
- Kept assertions behavioral and specific; no production code was changed.

## Verification

All commands run from `/Users/sri/Desktop/Desk/Finder/finder-pos`.

```text
cd web && npm test -- tests/catalogCart.test.tsx tests/reportsDashboard.test.tsx
PASS — 2 files, 12 tests

cd web && npm test
PASS — 9 files, 83 tests

cd web && npm run typecheck && npm run lint && npm run build
PASS
```

Lint still reports 4 pre-existing React hook warnings in:

- `app/(protected)/documents/_components/AllDocumentsTab.tsx`
- `app/(protected)/documents/_components/ByTypeTab.tsx`
- `components/terminal/OfflineQueueBanner.tsx`
- `components/terminal/TenderScreen.tsx`

Those warnings predate this queue item and were not changed.

## Honest status

Queue item #2 is complete. This improves frontend test trust, but it does not make the
application deployment-ready. The next release-critical work remains queue item #1:
core e2e failures against production build, mocks off, real backend and database.
