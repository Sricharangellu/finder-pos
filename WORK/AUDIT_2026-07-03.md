# Audit — 2026-07-03 (verification pass)

Immutable snapshot. Scope: full repo at commit `1ca7629` (master).
Run by: Claude (session with Sri). Follows `WORK/FORWARD_PLAN.md` Phase 1: Truth and cleanup.
Complements the narrative audit that produced the forward plan (same date); this file adds
the actual command results that audit did not run.

## Verification results

| Gate | Command | Result |
|---|---|---|
| Backend typecheck | `npm run typecheck` | ✅ PASS |
| Backend tests | `npm test` | ✅ PASS — 311/311, 0 fail (~3 min) |
| Frontend typecheck | `cd web && npm run typecheck` | ✅ PASS |
| Frontend lint | `cd web && npm run lint` | ✅ PASS after 2 fixes (below) — 0 errors, 4 warnings |
| Frontend unit tests | `cd web && npm test` | ❌ FAIL — 75/83 pass, **8 failures in 2 files** |
| Frontend build | `cd web && npm run build` | ✅ PASS — 143 routes compiled |
| Playwright e2e | `cd web && npm run test:e2e` | ⏸ NOT RUN — requires live Postgres + seeded env. 5 specs exist: login, checkout, invoice-pay, inventory-receive, verticals |

## Fixes applied during this audit

1. **`web/node_modules/es-abstract` was a corrupted install** (v1.24.2 present but missing
   its `2025/` directory) — this was the lint blocker reported in the forward-plan audit.
   Fixed by targeted reinstall (`rm -rf node_modules/es-abstract && npm install`).
   `package-lock.json` unchanged. Note: this is per-machine state; any other checkout may
   need the same repair or a clean `npm ci`.
2. **`web/app/(protected)/purchasing/edi-imports/_components/QueueTab.tsx:227`** — bare
   `// {line.parsed}` text node inside JSX (react/jsx-no-comment-textnodes error). Changed
   to explicit string literal. This was the only lint *error*; 4 `react-hooks/exhaustive-deps`
   warnings remain (non-blocking).

## Failing frontend tests (open defect — highest-priority follow-up)

Status label: **Built but not verified** — components changed, tests were not updated.

- `web/tests/catalogCart.test.tsx` — 5 failures (ProductGrid: render, category filter,
  search filter, empty state, onAddProduct click). Test expects product text (e.g. "Latte")
  that the current ProductGrid markup no longer renders the same way.
- `web/tests/reportsDashboard.test.tsx` — 3 failures (revenue KPIs, order counts/payment
  methods, top products). Same pattern: assertions against outdated markup.

Decide per test whether the component behavior or the test is correct, then fix the test
(or the regression). Do not delete tests to make the suite green.

## Repo hygiene findings

- `WORK_STATE 2.md` (stale 2026-06-29 snapshot, tracked) — **removed** this session; git history preserves it.
- Root `FINDER_APPLICATION_STATE_AND_FORWARD_PLAN.md` (untracked) — content moved to
  `WORK/FORWARD_PLAN.md` (timeframes removed, phase-based only); root copy deleted.
- `WORK_STATE.md` — moved to `WORK/WORK_STATE.md` per the new single-work-folder rule.
- `.next/`, `dist/`, `node_modules/` are **NOT** tracked in git (checked `git ls-files`) —
  the earlier audit's concern about tracked artifacts did not reproduce. 798 tracked files.
- No duplicate `.git` directories found (only `.git`, `.gitignore`, `.github`,
  `web/.gitignore`, `desktop/.gitignore`) — earlier concern did not reproduce.

## Honest caveats on the green gates

- Backend tests are unit-level with stubbed DB access (assertions like "issues UPDATE on
  suppliers"). They do **not** prove behavior against a live Postgres. Label for backend
  modules: **Built but not verified** (against real data). Live-DB integration + e2e proof
  is Phase 1/2 work.
- MSW mock mode is gated in `web/mocks/MockWorkerInit.tsx`: enabled when
  `NODE_ENV=development` OR `NEXT_PUBLIC_MOCK=true` OR URL param `?demo=1`. The `?demo=1`
  runtime switch ships in production bundles — acceptable for demos, but it means the
  mock layer is reachable in production builds. Forward plan already calls for build-time
  demo/production separation.
- The page-by-page mock-dependence matrix (which of the 143 routes work against the real
  backend vs MSW only) was NOT produced in this pass — it requires a live backend + DB.
  This remains the biggest unknown and the core of Phase 1.

## Module readiness matrix (module-level, this audit's evidence only)

| Area | Evidence | Label |
|---|---|---|
| Backend core domains (catalog, inventory, orders, payments, purchasing, customers, discounts, refunds, fulfillment, accounting, identity) | typecheck + 311 unit tests pass; 36 test files | Built but not verified (no live-DB proof this pass) |
| Backend orchestration (workflows, sagas, compensations) | dedicated passing unit tests incl. compensation paths | Built but not verified |
| Backend vertical modules (healthcare, hospitality, automotive, education, entertainment, manufacturing, appointments…) | compile; test coverage not assessed per-module | Partial / unassessed — expansion paused per forward plan |
| Frontend shell, routing, build (143 routes) | typecheck + production build pass | Built but not verified |
| Frontend POS terminal, catalog grid, reports dashboard | 8 failing unit tests in catalog grid + reports dashboard | Built but not verified; tests stale |
| Frontend ↔ backend integration per page | not measured (needs live stack) | Unknown — Phase 1 deliverable |
| E2E golden paths | specs exist, not executed | Built but not verified |
| RLS / tenant isolation / Redis rate limiting / metrics token / webhook secret encryption | not re-verified this pass; see forward plan security section | Not production-ready until proven |

## Next actions (phase order, no dates)

1. Fix the 8 stale frontend tests (`catalogCart.test.tsx`, `reportsDashboard.test.tsx`) → all local gates green.
2. Stand up local Postgres (docker-compose), run `npm run seed:e2e`, execute the 5 Playwright specs → first real end-to-end proof.
3. Build the page-by-page mock-dependence matrix against the live stack → completes Phase 1 truth.
4. Then Phase 2 (core release spine) per `WORK/FORWARD_PLAN.md`. No new modules/pages until Phase 2 exit criteria pass.
