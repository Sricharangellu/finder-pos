# FinderPOS — Engineering Audit 2026-07-04
> Session focus: Defect #1 investigation — orchestration layer vs real Postgres
> Predecessor: WORK/AUDIT_2026-07-03C.md (which already confirmed the fix is live)
> Note: Session C (Fable 5 agent, 2026-07-03 21:06 CDT) fixed defect #1 before
>       this session began. This audit records the independent verification
>       and documents the `db/migrations/0003_orchestration.sql` companion file
>       for the external psql migration path.

---

## Verification of session C's orchestration fix

Session C commit `54c2c2e` landed the following against the in-app migration system
(`src/orchestration/migrations.ts` + `app.ts` loop):

| Table | Approach |
|---|---|
| `workflow_instances` | Created in `src/orchestration/migrations.ts` |
| `workflow_instance_steps` | Created ibid (renamed from `workflow_steps` to avoid collision with workflowsModule) |
| `workflow_events` | Created ibid |
| `retry_state` | Created ibid |
| `workflow_locks` | Created ibid |
| `job_queue` | Created ibid |
| `event_outbox` | Created ibid (confirmed in code review) |

Command registration fix: `CommandRegistry.syncFromBus()` added — all 21 commands now
reported as registered at startup.

Gate results after session C commit (independently confirmed in this session):
- `npm run typecheck` → 0 errors ✅
- `npm run smoke` → 14/14 ✅ including new step 14 "zero failed workflow instances"
- `npm test` → 311/311 ✅

---

## New artifact: db/migrations/0003_orchestration.sql

Added `db/migrations/0003_orchestration.sql` for the **external psql migration path**
(`db/migrations/run.sh`). This is the companion to session C's in-app migration for
production deployments that use the standalone psql runner rather than the embedded
startup migration system.

Covers:
- `idempotency_keys` ALTER TABLE fixes (in-app schema was already correct post-session C;
  this handles the legacy 0001_foundation.sql schema used by psql deployments)
- All 7 orchestration tables matching the `workflow_instance_steps` naming convention
- RLS: fail-closed `current_setting('app.tenant_id')` per `db/rls/policies.sql` design intent
- All indexes matching what `src/orchestration/migrations.ts` uses

---

## Next priority (defect #2)

**e2e core-flow failures (10/47)** — from the 2026-07-03B audit:
- checkout ×3, inventory-receive ×3, invoice-pay ×3, logout ×1
- Session C claimed this as next work item (see WORK/LOCK.md)
- Run: `NEXT_PUBLIC_MOCK=false npx playwright test --project=chromium tests/core-flow.spec.ts`
- Target: all core-flow specs green against production build + real backend
